------------------ 트러블 슈팅 ------------------
T1. h_d 커맨드와 실제 서보가 height가 반대로 움직임 
S1. 시리얼 디버깅을 통해 theta_hips[i] 출력이 양의 방향으로 편향된걸 확인했고, POL.h [line 432]에 서보 각도 관련 하드 코딩된 값을 발견함. 이를 Params.h에 SERVO_LINK_ANGLE로 추가했고, 기존 theta_hips[i] 수식을 (5 * M_PI / 6) - angle_ABC; 에서 angle_ABC - (4 * M_PI / 6); 으로 변경함.
결과적으로 기존 (+5~-65도) 제어범위 h_d를 (-40~+45도)로 이동시킴.

T2. 수직 상태임에도 csv 로그에서 theta_eq - theta_hat > 2도가 관찰됨 
S2. IMU 바이어스 수정하고, theta_hips[i] 값을 역기구학 매트랩에 반영한 뒤 LQR 게인 (14개 샘플) 을 다시 뽑아 하드코딩함

T3. v = 0을 주면 뒤쪽으로 이동함. 즉, CoM은 로봇의 뒤쪽에 존재함. 
S3. parmas.h의 CoM_Body의 x값이 pitch에 직접 영향을 미치므로, 이 값을 앞으로 이동시키는 것으로 v=0을 줬을때 평형에 위치하도록 튜닝
직관에 위배되지 않도록, 커맨드에서 부호를 반전시켜서 인가
CoMx = 15: v = 0에서 평형 확정

T4. 

---------------------------- QnA -------------------------------
Q1. 핫스팟에 esp와 PC가 둘다 붙어있는 경우에만 csv 받을수 있는 이유?
A1. 핫스팟은 사설 IP (192.168.x.x) 를 단말에 할당하는데, 외부 라우터가 이 주소를 알 수 없기 때문.

 